classdef LeakageTrace
  properties (Access = 'private')
  end

  methods
    function pt = LeakageTrace()
    end

    function P = compute(values)
      %
      % Description:
      %
      %   Computes the total power dissipation as a certain moment of time
      %   that depends on a number of parameters.
      %
      % Input:
      %
      %   * step   - the index of the moment of time of interest,
      %   * values - the values of the parameters.
      %
    end
  end
end
